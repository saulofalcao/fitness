<%= form_for(@aula) do |f| %>
  <% if @aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aula.errors.count, "error") %> prohibited this aula from being saved:</h2>

      <ul>
      <% @aula.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_area :descricao %>
  </div>
  <div class="field">
    <%= f.label :professor_nome %><br>
    <%= f.collection_select(:professor_id, Professor.all, :id, :nome) %>
  </div>
  <div class="field">
    <%= f.label :horario %><br>
    <%= f.datetime_select :horario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
